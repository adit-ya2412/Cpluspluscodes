#include<iostream>
using namespace std;
string info();
void info(int*);
main()
{
	string n,fn,mn;
	n=info();
	fn=info();
	mn=info();
	int age;
	int fee;
	info(&age);
	info(&fee);
	if(age>=15&&age<=26)
	cout<<"\n eligible";
	else
	cou
}
}
